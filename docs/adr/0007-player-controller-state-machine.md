# 播放器控制器使用确认状态机

播放器后端返回的状态可能延迟、缺字段或短暂不一致，不能再让主流程用单次 `status()` 直接推断播放事实。播放器逻辑改由播放器控制器管理“播放观测”和“确认播放状态”：后端适配器只提供底层播放能力，控制器维护活动播放请求、暂停原因、播放确认和队列推进决策；聊天确认、AI 同曲判断和音乐队列数据操作仍留在主业务流程中。这样可以把不稳定播放器观测隔离在一个状态机里，避免 Web、点歌、自动出队等入口各自维护一套猜测规则。
