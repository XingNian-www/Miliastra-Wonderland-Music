# 运行时按依赖顺序启动和停止

应用组合入口先完成配置迁移与校验，再依次创建监控、计时、OCR、播放器、UI、聊天观察、业务、HTTP 与热键入口。停止时先关闭 HTTP、热键和新观察输入，再让业务运行时取消业务等待并持久化状态；当前 UI 例程只在已定义的终止清理边界取消，随后停止 UI、OCR、播放器、AI、计时和监控运行时并等待线程结束。watchdog 仍只属于二进制入口，正常关闭不触发异常重启。明确的依赖顺序避免后台线程持有已经销毁的端口或在退出时丢失队列状态。
