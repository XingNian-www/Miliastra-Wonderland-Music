# 聊天观察向 UI 运行时声明画面采样需求

UI 运行时是唯一截图所有者，但不拥有聊天变化检测与 OCR 节奏。聊天观察模块在监听驻留有效时声明画面采样需求，UI 运行时按需求截图并发布观察帧；活动 UI 例程内部产生且上下文匹配的画面可以满足同一需求。观察暂停时撤销需求，截图按需接口读取最新已发布帧。这样不会为 OCR、Web 和 UI 例程分别创建截图线程，也不会把观察策略塞进 UI 运行时。
