# 监控使用单一所有者投影

各运行时和业务模块发布类型化状态事件，由监控投影单一所有者维护最近日志、观察摘要、任务、队列、播放器、监听与业务公开状态。生产模块不再取得并修改共享 `MonitorShared`；Web 和 TUI 只读取同一份不可变快照。敏感文本在进入投影前已经脱敏，性能数据与事件日志继续分流。这样监控失败或读取变慢不会成为业务状态锁，也不会要求每个模块了解 Web/TUI 字段布局。
