# 候选歌曲审核使用独立 Provider

候选歌曲审核发生在点歌搜索和匹配已经产生最终候选歌曲之后、播放或入队之前，因为此时同时拥有原始点歌文本、候选歌名、歌手、来源和 URI，信息量比只审核点歌意图更完整。审核使用独立于 AI 点歌、支持 OpenAI Responses API 的 Provider，通过标准 `web_search` 工具获取公开信息，并以 1 到 10 的审核强度与本地阈值决定是否通过；这样可以接入联网审核或更严格的风控模型，同时避免把点歌候选选择和歌曲内容审核耦合在同一个 AI 配置里。
