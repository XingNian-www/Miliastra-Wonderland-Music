# 单一业务运行时拥有所有纵向业务状态

命令路由、业务调度器、娱乐互斥和各纵向业务模块状态统一由一个专用业务运行时线程拥有并顺序调用，而不是为每个玩法创建线程。聊天观察、计时器、AI、播放器和 UI 运行时通过类型化消息提交事件或返回结果；业务等待只保存状态，不阻塞业务线程，耗时外部操作仍由对应运行时执行。这样可以移除斗地主、谁是卧底和海龟汤等状态上的大量共享互斥锁，同时保持跨模块业务顺序确定。
