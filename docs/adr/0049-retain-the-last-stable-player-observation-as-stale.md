# 播放器异常时暂时保留过期稳定观测

已经形成稳定 URI 后，单次空 URI、请求失败或不完整样本不会立即清空歌曲身份；播放器观测运行时暂时返回上一条稳定观测，并明确标记过期状态和时长。过期上限按单调时间计算，不按失败样本数计算，因此不会受普通监控与快速确认采样间隔不同的影响；默认上限为 5 秒，并允许独立配置。过期观测不能用于确认新播放或自动出队；相同 URI 恢复后重新变为新鲜观测，不同 URI 需要重新稳定，超过配置上限后歌曲身份变为未知。这样可以过滤接口瞬时抖动，又不会长期把旧状态当作现实状态。
