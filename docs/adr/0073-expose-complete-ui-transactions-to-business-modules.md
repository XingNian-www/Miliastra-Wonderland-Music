# 业务模块只调用完整的 UI 机械事务

UI 自动化内核可以在内部复用打开唯一好友聊天等子例程，但业务模块不能获得只留下中间界面的独立契约。业务可调用的 UI 例程必须在一次独占执行中，从声明的界面前置条件完成目标输入并确认稳定结束状态后才返回；只有等待玩家、AI 或计时结果时才结束当前机械事务，并由后续结果提交新的事务。接受面向具体业务目标定义更多类型化契约的成本，是为了防止多个公开例程之间被其他 UI 请求插入，造成会话错位或结果无法归因。
