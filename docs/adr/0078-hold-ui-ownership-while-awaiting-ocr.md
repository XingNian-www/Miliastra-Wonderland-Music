# UI 例程等待 OCR 时保持窗口独占

UI 例程在 UI 运行时中完整执行，并获得只包含窗口观察与输入、视觉定位、类型化 OCR 确认和时间控制的窄执行上下文。模板匹配留在视觉定位模块，OCR 推理由独立 OCR 运行时拥有；例程等待 UI 确认 OCR 返回时继续保持 UI 所有权，业务模块和 `UiDevice` 都不能访问 OCR 实现。接受 OCR 等待期间其他 UI 工作排队的成本，是为了保证截图、识别、点击和确认属于同一画面因果链，避免识别完成前界面已被其他请求改变。
