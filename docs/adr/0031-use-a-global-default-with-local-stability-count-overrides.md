# 稳定次数使用全局默认值和局部覆盖

所有需要连续多次确认的模板、UI 状态、OCR 昵称和正文识别默认继承同一个全局稳定次数；具体模块或目标可以显式设置自己的次数。解析时优先采用大于 1 的局部值，否则采用大于 1 的全局值；两者都没有有效自定义值时使用内置默认值 2。因此缺省、0 或 1 都表示继续继承，而不是把局部确认次数降为 1。统一的只是默认连续次数，匹配阈值、超时、像素差异标准和文本判定规则仍由各自模块拥有。
