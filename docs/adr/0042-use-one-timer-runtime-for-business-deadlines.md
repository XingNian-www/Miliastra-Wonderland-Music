# 业务期限统一由计时运行时管理

娱乐阶段截止、周期提醒和空闲结算等期限统一提交给一个计时运行时，不再由各业务模块启动线程或依赖固定频率 `tick`。请求携带业务操作标识、会话代数和基于单调时钟的截止时间；到期后计时运行时只发送事件。业务模块负责取消、重新安排以及决定是否继续提醒，计时运行时不包含任何玩法规则。

成语、斗地主、跑得快、谁是卧底和海龟汤的业务期限已经按这一边界接入：各服务只投影当前有效期限并处理到期事件，业务运行时负责同步暂停状态和把到期结果转换为待发送效果。扫描循环只负责提交时钟活动状态并读取已产生的效果，不再在循环里推进玩法规则。
