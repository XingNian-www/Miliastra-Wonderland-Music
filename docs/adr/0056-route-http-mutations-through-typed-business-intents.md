# HTTP 状态变更统一转换为类型化业务意图

现有 HTTP 路径、方法、请求字段、响应字段和任务 ID 语义在不阻碍模块边界时保持兼容。HTTP 业务适配器只负责鉴权、输入形状校验和协议转换；播放控制、点歌、队列修改、海龟汤、谁是卧底、启动与监听切换全部提交类型化业务意图，不能伪造聊天文本，也不能直接修改共享状态。只读接口通过模块查询端口或监控快照读取；高级 UI 工具保持低优先级，截图读取最新观察帧。这样保留外部 API，同时移除 HTTP 对 `ApplicationRuntime` 内部对象的直接依赖。
