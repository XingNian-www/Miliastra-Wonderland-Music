# UI 例程在事务内归一化起点

每个公开 UI 例程必须在同一次独占事务内观察当前界面，并从该例程明确支持的稳定界面自行转换到目标动作所需起点；业务模块不能先调用独立的界面准备例程。遇到未知或过渡画面时，例程只进行有限复检，仍无法判断就以输入前失败结束，不盲目发送 `Esc` 或其他恢复输入。接受不同例程各自声明支持起点的成本，是为了防止准备与目标动作之间被其他 UI 请求插入，并把界面导航和安全恢复保持在 UI 自动化内核中。
