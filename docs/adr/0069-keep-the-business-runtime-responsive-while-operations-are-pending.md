# 外部操作等待期间业务运行时保持响应

业务运行时不能同步阻塞等待 UI、AI、播放器或计时结果。业务应用服务提交带业务操作标识和会话代数的请求后保存挂起业务操作并返回事件循环，结果到达时再恢复所属模块。一个挂起的正式任务仍占据正式任务顺位，因此不会启动下一项正式或延迟 UI 工作；业务运行时仍可接收聊天、计时、监控和取消排队任务等事件。这样保持现有正式任务串行语义，同时避免长 UI 或外部请求让所有业务状态与期限停止推进。
