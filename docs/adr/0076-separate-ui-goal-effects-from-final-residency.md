# 分离 UI 目标效果与结束驻留结果

每个 UI 例程契约必须分别报告目标动作效果和结束驻留结果，只有两者都确认后才返回完整成功；恢复目标界面失败不能抹掉已经发生或可能发生的目标输入。未完整成功的结果仍保留该契约特有的效果确定性和进度，例如已发送消息条数或邀请点击是否未知，业务模块不得据此重复已经确认、部分完成或结果未知的目标动作。接受为各契约定义独立结果类型的成本，是为了避免 `Result<()>` 和通用效果枚举同时丢失业务所需的安全判断。
