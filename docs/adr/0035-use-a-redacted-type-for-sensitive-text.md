# 敏感内容使用默认脱敏的文本类型

谁是卧底词语、秘密投票、海龟汤汤底等内容不能作为普通字符串跨模块传递，而应使用默认日志、调试输出和监控序列化均为固定脱敏内容的敏感文本类型。OCR 运行时保持业务无关并返回普通识别结果；独占决策读者或业务输入适配器在最早知道用途的边界立即把原文包装为敏感文本。只有拥有对应业务结果的模块可以显式读取原文；普通大厅聊天继续使用普通文本。这样可以避免秘密先以普通字符串跨越多个模块，也防止新增错误上下文或监控字段时意外泄露内容。
