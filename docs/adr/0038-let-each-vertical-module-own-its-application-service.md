# 每个纵向业务模块拥有自己的应用服务

每个纵向业务模块同时包含纯领域状态和业务应用服务。应用服务负责命令解析与权限、调用领域规则，并把领域结果转换为聊天发送、UI 例程、计时器或持久化等共享能力调用；领域状态本身不接触外部能力。中央组合入口只装配依赖，不再维护 `deliver_undercover_deliveries()`、`finish_landlord_outcome()` 等逐业务翻译分支，从而让完整业务行为留在所属模块内。
