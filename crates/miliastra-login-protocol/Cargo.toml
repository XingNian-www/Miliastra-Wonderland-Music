[package]
name = "miliastra-login-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
