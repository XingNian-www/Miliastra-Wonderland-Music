# 分别跟踪播放器歌曲身份与播放状态稳定性

播放器观测运行时分别累计 URI 歌曲身份和 `playing`、`paused`、`stopped` 播放状态的稳定次数，不要求整条原始响应完全一致。进度、时长和音量变化只更新观测字段与证据，不重置 URI 或状态计数。因此观测可以明确表达“URI 已稳定但播放状态仍在过渡”，避免正常递增的进度让整条观测永远无法稳定。两类计数都默认使用全局稳定次数，并允许局部覆盖。
