# 分离单帧 UI 判定与连续稳定跟踪

视觉定位模块保持无状态，只返回当前观察帧的 UI 判定与证据；UI 运行时拥有统一的状态稳定跟踪器，根据连续帧累计公共状态并产生稳定进度。稳定进度按具体状态标签累计，同属一级界面的不同模板结果不能互相凑数，同一帧也不能重复累计。`unknown` 只表示过渡中：它重置当前候选，但不授权任何输入、不触发监听模式切换，也不能把上一稳定状态当作当前事实；调用方只能在自己的有界超时内继续等待。具体 UI 目标可以使用独立的目标确认条件，但不能改写公共状态跟踪器。这样可以避免模板算法保存跨帧会话，也避免多个调用方对同一界面各自计数后得到互相矛盾的公共状态。
