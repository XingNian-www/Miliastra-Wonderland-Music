# 邀请确认后的机械阶段在一个 UI 例程内完成

大厅邀请确认、拒绝或超时仍作为不占用 UI 的业务等待；得到同意、默认同意或确认当前为公共大厅后，确认目标好友、发送同意通知、执行邀请点击、输入可选密码、确认结果和恢复驻留目标必须在一个独占 UI 例程内完成。目标好友无法确认时不能邀请；目标已经确认但通知发送失败时保留原因并继续邀请，成功进入后只在驻留大厅报告通知警告，不重新打开旧会话重试。拒绝分支只提交普通好友投递，不进入邀请例程。接受为邀请定义独立完整契约的成本，是为了删除跨例程的 `keep_chat_open` 布尔状态，防止界面已经被其他请求改变后仍把旧好友会话当作可复用事实。
