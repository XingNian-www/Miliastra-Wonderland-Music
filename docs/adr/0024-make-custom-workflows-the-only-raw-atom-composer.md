# 只有自定义工作流可以直接组合原子动作

状态：已由 ADR 0083 取代。

内置业务只能调用类型化 UI 例程，不能自行构造原子动作列表。自定义工作流解释器是唯一例外：它先把全部配置步骤解析并校验为自定义动作计划，再把整份计划作为一个默认不可让位的 UI 例程提交。这样既保留用户配置机械流程的能力，又避免原子动作接口扩散到所有业务模块；计划中的等待也不会自动释放 UI，除非以后经过单独设计和确认。
