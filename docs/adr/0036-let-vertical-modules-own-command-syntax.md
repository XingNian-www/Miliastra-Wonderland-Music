# 纵向业务模块拥有各自的命令语法

聊天入口只生成包含观察消息标识、昵称、来源、原文和权限信息的命令信封，静态命令路由器选择唯一业务模块；点歌、邀请和各娱乐玩法分别解析自己的命令类型、参数与权限。中央层不再维护包含全部业务的 `UserCommand` 或巨型解析分支。HTTP API 直接构造相应业务意图，不通过伪造聊天文本复用字符串解析。这样命令语言、状态规则和结构化结果可以保留在同一个纵向模块中，同时避免引入动态插件系统。
