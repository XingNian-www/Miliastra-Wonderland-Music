# 建立 UI 驻留只用于驻留目标

保留一个以确认到达一级界面或二级当前大厅为目标的“建立 UI 驻留”契约，但只允许聊天监听模式切换和明确的独立恢复任务直接提交。发送、邀请、管理、读取和启动等业务不能先调用它准备界面，必须在自己的完整 UI 事务内归一化起点并复用内部驻留导航子例程；自定义工作流的驻留步骤也只属于当前机械段。接受监听切换与恢复任务拥有一个专用公开契约的例外，是为了保留必要能力，同时防止通用 `ensure_ui_residency` 再次把界面导航、重试和插入风险扩散到所有调用方。
