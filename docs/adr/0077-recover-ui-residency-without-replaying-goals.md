# UI 收尾恢复不得重放目标动作

目标动作进入成功、失败、部分完成或结果未知等终止状态后，UI 例程仍在同一次独占事务内重新观察界面，并只从可确认状态有限收敛到请求中的驻留目标；收尾阶段绝不能重复目标动作。窗口丢失或界面持续未知时必须停止输入，并把恢复结果与目标动作结果分别报告。接受失败路径增加一次受约束观察与恢复的成本，是为了尽量恢复聊天监听驻留，同时避免把恢复失败变成重复发送、重复邀请或盲按 `Esc`。
