# 多好友投递在一个 UI 例程内顺序完成

向一个或多个好友发送消息统一表示为有序好友投递批次，并在一个不可插入的 UI 例程中顺序执行；普通单人反馈只是只有一个收件人的批次。例程按好友记录投递进度，全部目标处理结束后只恢复一次请求中的驻留界面，已经完成、部分完成或结果未知的目标不能由同一请求重发。接受多人投递期间 UI 被更长时间占用的成本，是为了防止其他任务在发牌或发词等私密投递之间改变界面，并让批次失败能够精确保留已发生的效果。
