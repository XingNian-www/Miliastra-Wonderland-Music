# 播放器运行时分离观测、控制和搜索通道

FeelUOwn 每次 RPC 都建立独立 TCP 连接，搜索或控制调用最长可能阻塞到 RPC 超时。播放器运行时因此使用三条有界执行通道：观测通道独占稳定状态并按正常约 1000ms 的节奏采样；存在带操作标识和截止时间的快速播放观察需求时临时切换到约 300ms，多个需求合并为一套采样；播放、暂停、继续、切歌和音量等修改操作在控制通道严格串行；候选搜索在独立的有界搜索通道执行。这样搜索或平滑音量不会中断稳定观测，同时不会让多个控制命令乱序。
